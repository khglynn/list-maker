# Dev + test dependencies for list-maker. Installs the runtime set first.
# CI (test.yml) installs this; the five scheduled workflows install requirements.txt only.
-r requirements.txt
pytest>=9.1.1             # unit tests for the pipeline guardrails (tests/)
pip-audit>=2.10.1          # the CI vulnerability gate (test.yml) — runtime deps only
